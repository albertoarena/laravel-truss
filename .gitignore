# Dependencies
/vendor/
composer.lock
/node_modules/
/package-lock.json

# Playwright
/test-results/
/playwright-report/
/.playwright/

# Test / tooling caches
/.phpunit.cache/
.phpunit.result.cache
/build/
/coverage/

# Private/local docs (note: the tracked docs/ folder is NOT ignored)
/.docs/

# IDE files
/.idea/
/.vscode/
*.iml

# Docs live demo: the frontend assets are copied from resources/ at build time
# (website/scripts/copy-demo-assets.mjs), so they are generated, not committed.
/website/public/demo/assets/

# Local Claude files
CLAUDE.local.md
CLAUDE.*.local.md
